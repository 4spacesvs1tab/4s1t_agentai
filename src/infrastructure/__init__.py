# Infrastructure layer — concrete adapters for external services.
# Adapters implement port interfaces defined in domain packages (e.g. kb/ports/).
# No domain logic lives here; only the translation between port and external system.
