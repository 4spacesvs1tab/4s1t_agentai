{
  "name": "get_current_datetime",
  "version": "1.0.0",
  "description": "Returns the current UTC date and time as an ISO 8601 string and Unix timestamp. Use this when you need to know the current time, date, or when generating time-stamped documents.",
  "agent_scope": ["ba_agent", "data_agent", "research_agent", "synthesis_agent"],
  "execution_mode": "subprocess",
  "timeout_seconds": 5,
  "max_memory_mb": 64,
  "network_allowed": false,
  "filesystem_access": "none",
  "secrets_required": [],
  "requires_approval": false,
  "input_schema": {
    "type": "object",
    "properties": {
      "format": {
        "type": "string",
        "description": "Optional strftime format string. Defaults to ISO 8601 if not provided.",
        "default": ""
      }
    },
    "required": []
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "iso": {
        "type": "string",
        "description": "ISO 8601 UTC datetime string (e.g. 2026-02-20T14:30:00+00:00)"
      },
      "unix": {
        "type": "integer",
        "description": "Unix timestamp (seconds since epoch)"
      },
      "formatted": {
        "type": "string",
        "description": "Datetime formatted with the requested format string, or ISO if not specified"
      }
    }
  }
}
